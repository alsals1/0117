def make_sum(p1, p2) : 
# 함수를 정의
# def로 시작
# 함수의 이름 적기. make_sum
# 괄호 안에 '매개변수 parameter'(함수를 정의할 때, 함수가 받을 값) 정의

# 함수 body
 
    """
    docstring(document string 주석), 함수를 설명하는 칸
    이것은 두 수를 받아 
    두 수의 합을 반환하는 함수입니다. 
    """
 
return p1 + p2 # 반환값. 함수는 return과 함께 작업이 '종료'된다. 

# return이 필수는 아니다


# 함수 호출

# '인자 argument'(함수를 호출할 때, 실제로 전달되는 값)



